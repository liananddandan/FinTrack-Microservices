name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check secrets presence
        run: |
          [ -n "${{ secrets.VPS_HOST }}" ] && echo "VPS_HOST exists" || echo "VPS_HOST missing"
          [ -n "${{ secrets.VPS_USERNAME }}" ] && echo "VPS_USERNAME exists" || echo "VPS_USERNAME missing"
          [ -n "${{ secrets.VPS_PORT }}" ] && echo "VPS_PORT exists" || echo "VPS_PORT missing"
          [ -n "${{ secrets.VPS_SSH_KEY }}" ] && echo "VPS_SSH_KEY exists" || echo "VPS_SSH_KEY missing"

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          port: ${{ secrets.VPS_PORT }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd ~/FinTrack-Microservices
            git pull
            docker compose up -d --build
            docker image prune -f